# 四阶段整体回归测试报告（2026-09-23）

本轮在提交 `6ab9e293d0359cc9aa6cbc1d0ad5a63a25257139` 上重新运行全量自动化测试，并在专用 PostgreSQL、Redis 和 Docker 环境中重放跨阶段主流程。机器可读结果见[整体回归 JSON](reports/four-stage-overall-2026-09-23.json)。本轮没有修改产品代码，也没有发起付费模型调用。

| 范围 | 本轮结果 | 实测路径 |
| --- | --- | --- |
| 阶段一：Agent 内核 | 包含在后端全量回归中 | Agent Loop、工具注册、Provider 事件、策略和确定性任务 |
| 阶段二：可靠执行 | 包含在后端全量回归和现场部署中 | API→独立 Worker→ToolEffect→Trace；故障恢复、租约、安全策略等测试 |
| 阶段三：Skill 生命周期 | 独立演示通过，数据集 24 案例 | 新库中发布第一版、拒绝退化候选、回滚并仅检索到恢复的 ACTIVE 版本 |
| 阶段四：扩展能力 | 四组 Demo 共 15 项通过 | 上下文压缩恢复、跨 Session 记忆与撤销、MCP 真实 SDK 调用、双 Worker/Redis 故障 |
| 全量后端 | **410 passed，13 skipped，0 failed** | PostgreSQL 15432、Redis 16379；127.82 秒 |
| 前端 | TypeScript 与生产构建通过；12 组件测试、6 浏览器流程通过 | 浏览器测试使用 HTTP fixture，不能代替实时后端联调 |
| Linux Docker 沙箱 | **11 passed，0 skipped** | 在 Linux 测试容器中连接 Docker，引擎 29.8.0；48.29 秒 |
| 数据库迁移 | 全新库与带旧消息的库均升级到 `20260922_0012`，`alembic check` 无漂移 | 旧版两条消息仍在，pgvector 0.8.1；另有独立全新库升级检查 |
| 部署联调 | API ready、UI 200、Worker 完成任务、已提交工具副作用、共享报告文件可读 | 专用 Compose `evoagent-v04-final`，Mock Provider，Memory/MCP 等新能力关闭 |
| 发布门禁 | 证据完整性无错误；`real_embedding` 待完成，默认退出码 2 | `release_ready=false`，版本仍是 `0.4.0.dev0` |

后端 13 项跳过中，11 项为另在 Linux 容器中全部通过的 Docker 专项；其余是 SQLite 无行锁语义 1 项、当前 Windows 无符号链接权限 1 项。全量 JUnit 有 4 条 `record_property` 与 `xunit2` 的格式提示，不影响测试判定；四组 Demo 使用 legacy JUnit 保留业务证据。原始 JUnit、Demo、迁移及部署输出保存在本地 `output/four-stage-*`，整体回归 JSON 记录其 SHA-256；原始输出未加入 Git。

阶段三演示使用确定性生成器，说明发布与回滚流程正常，不能证明 Skill 的真实效果。本轮未重复收费的 DeepSeek 实验；此前[真实算术 Skill 报告](reports/phase4-final-skill-deepseek.json)的 12 对实验中，基线和 Skill 都是 11/12，未显示提升。真实 Embedding 服务仍未配置，因此语义检索效果没有通过现场验收，不能宣称正式 v0.4 完成。

本次复用了模块 14 的专用 Compose 项目及其数据库卷，并新建了 `evoagent_overall_20260923` 等专用验收库。测试结束后停止 Compose 服务，保留卷中的实验记录；没有清理用户原有的 `output/`。
