# Agent 真实任务质量评测（2026-09-26）

这次评测使用与原有 18000 服务隔离的 PostgreSQL 库、Workspace 卷、API/Worker 容器和真实 DeepSeek 对话模型。评测前，16 个合成用户任务及 **80% 最低通过率**已单独提交为 `0608180`，数据集 SHA-256 为 `7e310097f8b78fc74a9481a388533e6a4710aec9203405b8b684d0b5c7653ffa`。执行后没有删题、改条件或调低门槛。使用的最新镜像 ID 为 `sha256:a73eee666b9eb6d1cb31b885eeb30818d0054f163c4fe233c4945b1e42ddf918`，API 是本机 `127.0.0.1:18001`，搜索为 Mock，记忆召回关闭。

命令：

```powershell
.\.venv\Scripts\python.exe scripts/agent_user_eval.py --api http://127.0.0.1:18001/api/v1 --output output/agent-core-user-v1-20260926.json
```

每个样本独立创建 Session，按顺序提交目标。程序只自动回答由 `ask_user` 发起的人工问答，不批准其他副作用；用任务终态、`acceptance.checked`、成功工具调用数量和 Artifact 数量判分。双轮样本在同一 Session 中验证历史延续。原始结构化结果已脱敏后保存为[评测报告](reports/agent-core-user-v1-2026-09-26.json)，冻结题目见[数据集](../evals/datasets/agent-core-user-v1.json)，运行器见[脚本](../scripts/agent_user_eval.py)。

| 类型 | 通过 / 总数 | 结果范围 |
| --- | ---: | --- |
| 单步 calculator | 4 / 6 | 两例耗尽模型网络错误重试 |
| 多次计算 | 2 / 2 | 至少两次成功工具调用，答案含各结果 |
| 文件写入 | 3 / 3 | 成功调用 `file_write`，文件位于对应 Run 目录 |
| Artifact | 1 / 1 | 成功调用 `artifact_write` 且生成记录 |
| 多轮历史 | 3 / 3 | 第二轮复述或计算第一轮所给信息 |
| 人工问答 | 1 / 1 | `ask_user` 暂停、提交“蓝色”后完成 |
| **合计** | **14 / 16（87.5%）** | **通过预设的 80% 门槛** |

失败样本 `arithmetic-03`（Task `6fc6a0cf-88c3-4b9d-9e2e-5a002d5f8294`）在一次尝试中已成功调用 calculator，但后续模型请求断线；`arithmetic-04`（Task `dda653f9-6942-4330-8e3b-41560978c371`）未取得成功工具调用。两者均自动重试到 `attempt_count=3` 后以 `provider_network_error` 失败；没有 `acceptance.checked`，因为模型未正常生成最终答复。使用 `frontend/scripts/verify-live-task.mjs` 从真实页面重新打开两条 Session，均显示“模型服务连接失败”，与 API/Trace 一致。网络错误可能来自服务端、链路或本机连接，本次证据不能定位根因；现有重试和明确失败语义生效，但用户任务仍会失败。

最新镜像还通过 `frontend/scripts/acceptance-live-browser.mjs` 在真实页面创建 Workspace `3115121c-172f-4a7b-8c42-5a72383fc4a9`，提交 Task `78890439-1b88-4d94-8988-ea94f6ac869a`，Run `ab2c7b45-c8ce-40c9-a241-a80aadf1eb13` 中 calculator 成功且验收通过。具体环境与初次通过/故意失败案例见[运行模式与验收记录](reports/agent-basics-live-2026-09-26.json)。

初次自动化回归：后端 PostgreSQL/Redis **426 passed、13 skipped**，前端 **12 Vitest、13 Playwright**，构建、Ruff 检查与格式检查通过。跳过项是 Linux Docker 专项 11、SQLite 行锁语义 1、Windows 符号链接权限 1。评测覆盖的是受控、可机械判分的 16 个样本，不能代表开放任务质量、事实准确性、安全性、成本分布或复杂 Skill/MCP/记忆组合。当前页面不会把验收条件作为模型指令注入，目标文本须明确写出要求。正式 v0.4 发布门槛维持未通过。

后续新增三条 PostgreSQL 双 Worker 运行中崩溃测试：备用 Worker 在主 Worker 被强制终止前已在线，只读、COMMITTED 和 UNKNOWN 三种边界均通过；API/Trace 与副作用调用次数一致。再用独立 Mock API/数据库把 UNKNOWN 交接接到真实页面，由用户在核对磁盘文件后选择 `committed:report.md`，最终只执行一次写入。另复验文件已写入后的工具超时：修复终态误判及审批后反复等待两个缺陷，页面确认已提交后完成，仍只写一次。已失败的工具检查点无法在同一任务安全重放；若核对后确认未提交，页面要求取消并创建新任务。[机器可读报告](reports/agent-dual-worker-crash-2026-09-26.json)保留失败与修复证据。另一条真实模型页面任务输入 20,000 个汉字，在请求前因 `context_budget_exceeded` 被拒绝，页面与 Trace 一致且未调用工具。联合测试范围仍是受控文件写入和超预算拒绝路径；无法由此放行所有外部工具、复杂长上下文质量或正式 v0.4。

最终代码全量回归更新为后端 PostgreSQL/Redis **430 passed、13 skipped**，前端 **12 Vitest、13 Playwright** 与构建通过。2026-09-27 的最终镜像 `sha256:dd8bfa17b442f47e8febd427d59c39daed922aa7460d6c8a321f2378a4d94bf5` 已在独立真实模型环境再次通过页面 Workspace 创建、calculator 与显式验收；Task `961034b1-2740-41c4-90c2-11427f7ad5fe`、Run `378d4940-29b9-414c-80d3-2289c20f79dd`。原 16 项冻结评测属于此前镜像，其数据集与结论不因最终烟测改变。
