# 阶段四模块 0～2：验收与运行说明

实现日期：2026-09-19。版本：`0.4.0.dev0`。本次“前三个模块”按指南编号指模块 0、1、2。

> 本文保留模块 0～2 交付时的验收记录。最新增量已实现模块 3～5，默认快照版本改为 2；后续部署与 API 以[模块 3～5 说明](阶段四-模块3至5验收与运行说明.md)为准。

## 交付范围

| 模块 | 已实现 | 验收边界 |
|---|---|---|
| 0：基线与恢复装配 | Worker 自动处理 RECOVERING；真实进程停止/接管测试；阶段三三种生命周期结果复核 | 本地 Mock/SQLite 已验证；真实模型配对报告、PostgreSQL 与容器验收仍待补充 |
| 1：租约代次与写保护 | epoch、启动 UUID、数据库时钟、同事务 Guard、全部未决效果恢复、多审批等待、取消清理、零命中冻结 | SQLite 验证各入口；PostgreSQL 行锁竞争用例已提供，本机未运行 |
| 2：请求预算与裁剪 | ContextBudget、TokenCounter、消息分组、每轮检查、输出上限、可选资料稳定裁剪、指纹与兼容模式 | 本地预算及错误路径已验证；真实兼容服务计数为 estimated，无模型 tokenizer 精确保证 |

没有实现模块 3 的自动摘要、ContextRevision 或完整输出归档，也没有实现长期记忆、MCP、向量检索和 Redis。

## 数据库升级

先停止旧版 Worker，防止不检查 epoch 的旧进程继续写入，再执行：

```powershell
.\.venv\Scripts\python -m pip install -e ".[dev]"
.\.venv\Scripts\alembic upgrade head
.\.venv\Scripts\evoagent-worker
```

新迁移 `20260919_0005` 增加 `tasks.lease_epoch` 和 `runs.skill_selection_frozen`，已有数据分别初始化为 0 和 false；已有配置快照或选择记录仍会冻结历史检索结果。不要在运行中混用新旧 Worker。备份并确认无活跃任务后才考虑降级；回退命令为 `alembic downgrade 20260908_0004`，会删除新增字段。

旧 Run 的代码版本或上下文配置指纹与新 Runtime 不同，会以 `snapshot_incompatible` 失败，不静默采用新预算续跑。历史报告仍可读取；要继续旧语义，应使用对应旧版本的隔离运行环境，或新建任务。

## 上下文配置

| 环境变量 | 默认 | 含义 |
|---|---|---|
| `EVOAGENT_CONTEXT_POLICY` | `bounded` | 每轮检查；`legacy` 显式保留旧请求行为 |
| `EVOAGENT_CONTEXT_WINDOW_TOKENS` | 32768 | Mock 示例窗口；真实 Provider 必须显式核对配置 |
| `EVOAGENT_MAX_OUTPUT_TOKENS` | 4096 | 本轮输出预留，也是 Provider 实际输出上限 |
| `EVOAGENT_CONTEXT_SAFETY_MARGIN` | 1024 | 输入之外的安全余量 |
| `EVOAGENT_CONTEXT_STRICT` | false | true 时拒绝未验证计数方式；当前真实适配器会返回 `context_count_unverified` |
| `EVOAGENT_MAX_TOTAL_TOKENS` | 32000 | 原有 Run 累计消耗软预算，与窗口不同 |

必须满足窗口大于输出预留与余量之和。可选资料通过 ContextBuilder 的 `external_context` 显式标记；不会按用户文本中的“外部上下文”字样删除任务。数值较小的 priority 先裁剪；工具调用和对应结果完整保留。裁剪后仍超限时返回 `limit_reached/context_budget_exceeded`，没有调用模型。

显式兼容 CLI：

```powershell
$env:EVOAGENT_CONTEXT_POLICY = 'legacy'
.\.venv\Scripts\evoagent --demo
Remove-Item Env:EVOAGENT_CONTEXT_POLICY
```

## 本地证据

- 修改前回归：207 passed、4 skipped。
- 修改后全量回归：**228 passed、10 skipped（44.33 秒）**；Ruff lint/format、`git diff --check` 通过；默认 bounded CLI Demo 正常。10 项跳过为 8 项缺 PostgreSQL 配置、1 项 SQLite 不支持行锁语义、1 项 Windows 符号链接权限不足。
- `tests/e2e/test_worker_process_recovery.py`：从 API 创建任务，子进程运行真实 JobWorker/ConfiguredTaskHandler，在指定边界强制终止进程；同标签替代进程自动恢复。覆盖只读续跑、COMMITTED 复用且文件只写一次、UNKNOWN 等待确认。
- `tests/integration/test_lease_fencing.py`：同标签新 epoch 接管后，旧事件/快照/工具前后处理/配置/检索/Artifact/心跳/终态均被拒绝；合法新租约可继续写入。还覆盖多审批、取消清理、零命中冻结和 PostgreSQL 事务锁。
- `tests/unit/test_context_policy.py`：中英文大文本、大量 Schema、保护任务、防前缀误删、完整工具组、分区优先级、第二轮超限、取消、legacy、严格/估算计数。
- `tests/integration/test_persistent_runtime.py`：预算配置改变后，恢复失败并落库，Provider 没有收到请求。
- SQLite migration 已覆盖 head/base 往返及 metadata 差异检查。

阶段三 Demo 在全新 `workspace/phase4-baseline-9a283b03/` 运行，完整结果保存为 `demo-result.json`，Artifact 保存在同目录的 `artifacts/` 下。Workspace 不入版本库，下列事实便于跨机器核对：

| 事实 | 本次结果 |
|---|---|
| 数据集 | 24 个 Case |
| 第一版 | gate_passed=true，published=true |
| 退化版 | gate_passed=false |
| 第三版 | new_gate_passed=true |
| 回滚 | ACTIVE 恢复为第一版；检索只命中第一版 |
| 第一版 version_id | `5a5e30c5-aebc-4baa-803c-e2e1babe805a` |
| 正常/退化/回滚前实验 | `74122b5b-484e-49cd-a0de-4eab077cf6d9` / `32dc6614-8fa0-466f-8764-5bf88d802909` / `9965ef8e-4f7f-4545-a0e8-ae9981cc7cc3` |

这是确定性模拟完成记录的生命周期 Demo，不是真实模型配对效果报告。

## 待补充的环境验收

本机没有配置 `EVOAGENT_TEST_DATABASE_URL`，未发现 PostgreSQL 服务、Docker 命令，且未找到可认定为真实模型配对报告的交付证据。相关验收保持待完成，不用 SQLite 或模拟指标代替。

在专用、允许测试建表/删表的 PostgreSQL 数据库上运行：

```powershell
$env:EVOAGENT_TEST_DATABASE_URL = 'postgresql+asyncpg://<user>:<password>@127.0.0.1:5432/evoagent_test'
.\.venv\Scripts\pytest -m postgres -q
```

现有 CI 已提供 PostgreSQL 17，并在 Python 3.12/3.13 下运行全套测试；本次未提交或触发远端 CI，不能引用历史配置作为本次成功结果。容器验证仍需 `docker compose config` 与镜像构建。真实配对报告需另行按阶段三运行说明完成，保存配置指纹、报告哈希与失败样本。

因此本次登记为“模块 0～2 代码及本地验收完成，外部环境验收待补”，不是阶段四整体完成或生产并发已验收。
